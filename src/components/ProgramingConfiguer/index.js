import React, { memo } from 'react';

const ProgramingConfiguer = (props) => {
  const { title, onOk, onCancel, programing } = props;

  return <div>ProgramingConfiguer</div>;
};
export default memo(ProgramingConfiguer);
