import React, { memo } from 'react';

const RefreshPage = () => {
  return (
    <div
      style={{
        height: '100%',
        background: '#fff',
      }}
    >
      RefreshPage
    </div>
  );
};
export default memo(RefreshPage);
