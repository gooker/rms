import React, { memo } from 'react';

const RefreshPage = () => {
  return (
    <div
      style={{
        height: '100%',
        background: '#fff',
      }}
    />
  );
};
export default memo(RefreshPage);
