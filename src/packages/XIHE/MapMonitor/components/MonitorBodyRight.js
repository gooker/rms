import React, { memo } from 'react';
import commonStyles from '@/common.module.less';

const MonitorBodyRight = (props) => {
  const {} = props;
  return <div className={commonStyles.mapBodyRight}>111</div>;
};
export default memo(MonitorBodyRight);
