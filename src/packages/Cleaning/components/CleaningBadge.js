import React, { useState } from 'react';

// import styles from '../index.less';
const spanStyle = {
  position: 'absolute',
  left: 0,
  top: 0,
  //   backgroundColor:'pink',
};
function CleaningBadge() {
  const [show, setShow] = React.useState(true);

  return <span>CleaningBadge</span>;
}
