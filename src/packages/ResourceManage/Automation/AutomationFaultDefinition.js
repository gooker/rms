import React, { memo } from 'react';

const AutomationFaultDefinition = (props) => {
  const {} = props;
  return <div>AutomationFaultDefinition</div>;
};
export default memo(AutomationFaultDefinition);
